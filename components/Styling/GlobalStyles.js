// GlobalStyles.js

import { StyleSheet } from 'react-native';

const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ffefd5',
    justifyContent: "center", 
    alignItems: "center" // Ændre baggrundsfarven efter behov
    // Tilføj andre globale stilarter her
  },
  // Tilføj flere globale stilarter her
});

export default GlobalStyles;
